name: CI

on: [push, pull_request]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

defaults:
  run:
    shell: bash

jobs:

  fypp:

    timeout-minutes: 10

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v5

      - uses: actions/setup-python@v6
        with:
          python-version: '3.x'
          cache: 'pip'

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run fypp (src)
        run: |
          cd src
          fypp -F elliptic_k_using_agm_fortran.fypp elliptic_k_using_agm_fortran.f90
          cd ..

      - name: Upload artifacts (src)
        uses: actions/upload-artifact@v4
        with:
          name: src-f90
          path: src/*.f90
          retention-days: 1

  test:

    needs: fypp

    timeout-minutes: 10

    runs-on: ubuntu-latest

    strategy:
      matrix:
        profile: [debug, release]
      fail-fast: false

    steps:

      - uses: actions/checkout@v5

      - name: Install fpm
        uses: fortran-lang/setup-fpm@v8
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}

      - name: Build library using fpm
        run: fpm build --profile ${{ matrix.profile }}

      - name: Test library using fpm (${{ matrix.profile }} mode)
        run: fpm test --profile ${{ matrix.profile }}
